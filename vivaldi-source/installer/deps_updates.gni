import("//vivaldi/installer/mini_installer/deps_updates.gni")

if (is_linux && !is_component_build) {
  update_target("//chrome/installer/linux:linux") {
    deps -= [
      ":beta",
      ":canary",
      ":deb_repo_package",
    ]
  }
}
